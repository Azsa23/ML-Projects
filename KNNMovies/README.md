A simple Python project that recommends similar movies using the K-Nearest Neighbors (KNN) algorithm and Scikit-Learn.

## 🚀 How to Run
1. Install the required libraries:
   ```bash
   pip install pandas scikit-learn
